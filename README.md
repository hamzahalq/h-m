# Bith.ai Marketing Agent

MVP scaffold for Team H&M hackathon project.

## Backend

```bash
cd backend
pip install -r ../requirements.txt
uvicorn main:app --reload --port 8000
```

## Frontend

```bash
cd frontend
pip install -r ../requirements.txt
streamlit run app.py
```
